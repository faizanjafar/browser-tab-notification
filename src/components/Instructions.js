import React from "react";

const Instructions = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        padding: "1em",
        marginBottom: "1em",
        lineHeight: "1.4",
        background: "moccasin",
        textAlign: "center",
      }}
    >
      Use the buttons below to change the title tag and favicon
    </div>
  );
};

export default Instructions;
